name: 'Qualys IaC Security (Enhanced)'
author: 'nelssec'
description: 'Scan IaC templates with Qualys CloudView. Adds policy_name support for custom CSA policy evaluation.'
branding:
  icon: 'shield'
  color: 'blue'
inputs:
  directory:
    description: 'Directory with infrastructure code to scan'
    default: '.'
    required: false
  policy_name:
    description: 'Qualys CSA policy name (Build time execution type) to evaluate against. If omitted, the default policy is used.'
    required: false
    default: ''

runs:
  using: 'docker'
  image: 'dockerfile'
  args:
    - ${{ inputs.directory }}
    - ${{ inputs.policy_name }}
